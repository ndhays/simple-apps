{
  "name": "slideshow",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron-builder --mac --project apps/slideshow"
      },
      "configurations": {
        "release": {
          "command": "npx electron-builder --mac --publish always --project apps/slideshow"
        }
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron .",
        "cwd": "apps/slideshow"
      }
    }
  }
}
